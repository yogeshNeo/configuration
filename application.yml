spring:
  datasource:
    url: jdbc:mysql://localhost:3306/orderTestDb?createDatabaseIfNotExist=true
    username: root
    password: 123456
  cloud:
    config:
      refreshInterval: 10s

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: orders-group
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      
message: Project configuration loads from external config file for changed pwd

management:
  endpoints:
    web:
      exposure:
        include: "*"
